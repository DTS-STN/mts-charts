---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: pvc-confluence-attachments
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Gi
  storageClassName: "default"
  volumeName: pv-confluence-attachments
