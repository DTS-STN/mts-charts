apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dts-smtp.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "dts-smtp.name" . }}
    helm.sh/chart: {{ include "dts-smtp.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "dts-smtp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
